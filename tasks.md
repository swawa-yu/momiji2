# シラバス検索システム レビューメモ (2025-04-12)

## CSS に関する点

- [ ] **`!important` の削減**:
    - 箇所: `src/components/Timetable/Timetable.css` の `.term-selected`
    - 検討事項: 詳細度の高いセレクタや読み込み順の調整による代替。
- [ ] **CSS 変数の活用促進**:
    - `index.css` で定義済みの変数をコンポーネント固有 CSS でも活用する。
    - 例: `SearchComponent.css` の `.do-search button` 背景色など。
    - 目的: テーマ変更への追従性向上。
- [ ] **セレクタとクラス名**:
    - 検討事項: より明確な命名規則（BEM など）の導入、または CSS Modules / Styled Components の活用。
    - 目的: スタイル競合の防止、保守性向上。
- [ ] **レイアウト (Flexbox/Grid)**:
    - 検討事項: Flexbox/Grid をさらに活用し、レスポンシブ対応を強化する。
    - 例: `SearchComponent.css` の `.search-groups` など。
- [ ] **絶対配置の見直し**:
    - 箇所: `App.css`, `Timetable.css`, `ExportBookmarkButton.css` など ( `position: absolute`/`fixed`)
    - 検討事項: 他のレイアウト手法（Flexbox/Grid）での代替可能性。
    - 目的: レイアウト崩れリスクの低減。

## TypeScript とロジックに関する点

- [ ] **型定義 (`src/types/`)**:
    - 検討事項: `YoubiKoma` 型の命名再考（例: `TimeSlotIdentifier`）、または型の分離。
    - 検討事項: `KaikouBukyoku` など多数の文字列リテラル型の管理方法（動的生成、別ファイル化）。
- [ ] **データパース (`src/subject/utils.ts`)**:
    - 改善点: パース失敗時のエラーハンドリング強化（失敗理由の明確化、カスタムエラー、正規表現活用）。
    - TODO: より多くの実データでのテスト、未対応パターンの洗い出し。
- [ ] **検索ロジック (`src/search/index.ts`)**:
    - 改善点: `matchesYoubiKoma` 関数の可読性向上（ロジック整理、ヘルパー関数切り出し）。
    - 懸念点（将来）: データ量増加時のパフォーマンス。Web Workers やサーバーサイド処理を視野に入れる。
- [ ] **初期化処理 (`src/subject/index.ts`)**:
    - 推奨: 非同期処理には `async/await` を活用する。

## UI/UX とデザインに関する提案

- [ ] **UI ライブラリの活用検討**:
    - 候補: MUI, Chakra UI, Ant Design など。
    - 目的: デザイン一貫性、レスポンシブ対応、アクセシビリティ向上。
- [ ] **検索インターフェース改善**:
    - 検討事項: 検索条件のグループ化、アコーディオン UI 導入による視認性・操作性向上。
- [ ] **テーブル表示 (`TableView`) 改善**:
    - 検討事項: 高機能テーブルライブラリ (TanStack Table など) の活用（ソート、フィルタリング、ページネーション）。
    - 検討事項: 表示列カスタマイズ機能、重要情報の強調。
    - 検討事項: `abstract` (授業概要) の表示方法見直し（文字サイズ、詳細表示ボタン/モーダル）。
- [ ] **時間割表示 (`Timetable`) 改善**:
    - 検討事項: フローティングウィンドウのインタラクション改善（表示/非表示、ドラッグ移動）。
    - 検討事項: 他の UI パターン（モーダルなど）の検討。

## 次のステップ

- [ ] **ユーザー**: デザインに関する具体的な悩みや改善したい点を明確にする。
- [ ] **ユーザー**: 必要に応じて主要な React コンポーネント (`.tsx`) のコードを共有する。
- [ ] **ユーザー**: 改善の優先順位を決定する。